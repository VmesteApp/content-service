[DEFAULT]

%include = yoyo-local.ini

sources = migrations

database = postgresql://%(DB_USER)s:%(DB_PASSWORD)s@%(DB_HOST)s:%(DB_PORT)s/%(DB_NAME)s

verbosity = 3

batch_mode = off

editor = %EDITOR

post_create_command = hg add {}

prefix = content_service
